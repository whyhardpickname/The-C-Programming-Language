#include<stdio.h>

#define MAXLINE 100 //最大输入行

int getLine(char line[], int max);
int strindex(char s[], char t[]);
int main()
{
    
    return 0;
}
//TODO
// -getline函数
// -strindex函数
/* 
    该函数读取输入文本的一行,并把当前行保存在字符数组line中.
    
    参数
    line    存储文本每行内容的字符数组
    max     字符组数最大长度,包括'\0'

    返回
        1   当输入文本存在下一行,并把当前行保存在line中.
        -1  当输入文本不存在下一行.
 */
int getLine(char line[], int max)
{
    return -1;
}
/* 
    该函数返回字符串t在字符串s中第一次出现的位置的下表索引 */
int strindex(char s[], char t[])
{
    return -1;
}